[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.1"
sqlx = { version = "0.8.5", features = ["runtime-tokio", "sqlite", "macros", "sqlx-macros", "migrate"] }
csv = "1.3.1"

