use std::io;
use backend::stream_io::StreamIO;
use crate::new::router::Router;

pub struct PublisherManager {}

impl PublisherManager {
    pub fn new() -> Self {
        todo!()
    }

    pub fn register_publisher(&mut self, publisher: StreamIO) {
        todo!()
    }

    pub fn receive(&mut self, router: &mut Router) {
        todo!()
    }
}
